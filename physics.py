def read_vars(input_file_name):
    """Read the next line from the variable value input file
        Each line specifies values to assign each variable in the equation 
        The input file will be in CSV format.
    """
    #open file
    #read the next line for values
    #return the values


def physics_equation(a, b ,c):
    """Insert physics equation here
    """

    result = equation(a, b, c)

    return result


def write_result(output_file_name, result):
    """Write to the output file with the result of the equation
    """

